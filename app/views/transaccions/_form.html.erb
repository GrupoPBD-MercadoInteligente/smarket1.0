<%= form_for(@transaccion) do |f| %>
  <% if @transaccion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@transaccion.errors.count, "error") %> prohibited this transaccion from being saved:</h2>

      <ul>
      <% @transaccion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :transaccion_id %><br>
    <%= f.text_field :transaccion_id %>
  </div>
  <div class="field">
    <%= f.label :tipo_trans_id %><br>
    <%= f.text_field :tipo_trans_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
